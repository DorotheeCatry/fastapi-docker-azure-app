name: Loan Prediction Service CI

on:
  push:
    branches:
      - tests
  pull_request:
    branches:
      - tests

jobs:
  test:
    runs-on: ubuntu-latest
    environment: Test

    env:
      # Variables d'environnement pour la base de données et les secrets
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      SECRET_KEY: ${{ secrets.SECRET_KEY }}
      ACCESS_TOKEN_EXPIRE_MINUTES: ${{ secrets.ACCESS_TOKEN_EXPIRE_MINUTES }}
      ALGORITHM: ${{ secrets.ALGORITHM }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov httpx pytest-httpx

      # (Facultatif) Debug : afficher DATABASE_URL pour vérifier qu'il est bien passé
      - name: Debug environment variables
        run: |
          echo "DATABASE_URL=$DATABASE_URL"
          echo "SECRET_KEY=$SECRET_KEY"
          echo "ACCESS_TOKEN_EXPIRE_MINUTES=$ACCESS_TOKEN_EXPIRE_MINUTES"
          echo "ALGORITHM=$ALGORITHM"


      - name: Run tests with coverage
        run: |
          pytest --cov=app --cov-report=xml --cov-report=html

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v3
        with:
          file: ./coverage.xml
          fail_ci_if_error: true
